B-107 staged fixtures. This directory is deliberately outside every runner
discovery root (tests/cases, tests/cases/{llvm,native_only,modules,verify_rc,
negative,errors}); nothing here executes in any suite.

Every directory here encodes target behaviour of the resolver-plan pipeline
that the checker does not deliver yet. Each was verified to fail identically
on the main anchor compiler (2026-07-31), so none is a regression of the
b107 branch. When a fixture's gate closes, move it into
tests/cases/modules/ to activate it; do not force-green it before then.

Gate legend:
  [G1] checker adoption of the plan for inline/relative imports
       (use super::... inside mod blocks still resolves through the legacy
       infer_ctx path: old E0201/E0707 message shapes, no E0702/E0703/E0704
       classification, no same-payload cycle resolution).
  [G2] plan construction for single-file / no-dependency entry points
       (today the plan machinery only runs for multi-file module graphs).

Positive fixtures (expected to turn green at the gate):
  plan_namespace_duplicate_enum_relation      [G1] one legal `mod same` owns a
      single enum relation whose two variants remain available through
      `pub use super::same::{Shared}`. Legacy path: E0201.
  project_namespace_direct_enum_qualified     [G2] three-segment qualified
      `facade::State::Hit(17)` in a single file must resolve the exact member
      and never fall back to a root decoy fn. Legacy path: leaf fallback,
      E0301 against the decoy.
  project_namespace_same_payload_value_cycle  [G1] a/b/c re-export cycle where
      every `V` has the same ultimate payload must resolve without issues
      (plan-level behaviour proven by the unit3c harness). Legacy path: E0201.

Negative fixtures (expected to start failing with the new diagnostics at the
gate; today the legacy path either accepts the program or reports a different
code/shape):
  error_namespace_duplicate_source_import     no-partial-module E0207 at the
      second direct `mod same`; its members must never seed the plan
  error_namespace_enum_member_exact_collision [G1][G2] exact E::V collision, E0707
  error_namespace_inline_ambiguity            [G1] plan-format E0707 payload list
  error_namespace_inline_missing_frame        [G1] E0702 (legacy: E0201)
  error_namespace_inline_missing_symbol       [G1] E0703 (legacy: E0201)
  error_namespace_inline_obligation_scc       [G1] E0704 (legacy: E0201)
  error_namespace_inline_over_super           [G1] normalized E0705 message
  error_namespace_nonvalue_growth_cycle       [G1][G2] growth-guard E0704
  error_namespace_projected_seed_collision    [G1][G2] one legal module with
      a direct Struct/ExternType source collision, E0207 rather than E0707
  error_namespace_resolved_value_cycle        [G1] seedless cycle E0704
  error_namespace_value_growth_cycle          [G1][G2] growth-guard E0704
  error_namespace_value_witness_nonpollution  [G1] one exact E0704, no spray
  error_project_private_inline_enum_ctor_leak [G1][G2] private-leak rejection
  error_project_private_inline_extern_leak    [G1][G2] private-leak rejection
