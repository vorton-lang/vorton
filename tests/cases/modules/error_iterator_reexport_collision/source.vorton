pub struct CollisionIter {}

impl Iterator for CollisionIter {
    type Item = Int
    fn next(mut self) -> Int? { none }
}

pub struct CollisionSource {}

impl Iterable for CollisionSource {
    type Item = Int
    type Iter = CollisionIter
    fn iter(self) -> CollisionIter { CollisionIter {} }
}
