use left::{make as make_left}
use right::{make as make_right}

fn main() {
    let mut left_counter = make_left()
    left_counter.adjust()
    print(left_counter.read())

    let mut right_counter = make_right()
    right_counter.adjust()
    print(right_counter.read())
}
