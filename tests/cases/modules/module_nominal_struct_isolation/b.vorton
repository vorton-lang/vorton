struct Packet {
    label: Str,
    code: Int,
}

pub fn make_packet(label: Str, code: Int) -> Packet {
    Packet { label: label, code: code }
}

pub fn read_packet(packet: Packet) -> Int {
    packet.code
}
