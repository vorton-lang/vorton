enum Token {
    Item(Int),
}

pub fn make_token(value: Int) -> Token {
    Token::Item(value)
}

pub fn read_token(token: Token) -> Int {
    match token {
        Token::Item(value) => value,
    }
}
