use mut_lib::{touch as increment}
use effect_lib::{touch as risky_touch}

fn main() {
    let mut n = 0
    increment(n)
    print(n)

    let recovered = risky_touch(7) catch { _ => 7 }
    print(recovered)
}
