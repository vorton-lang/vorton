use model::{Payload, ResultShape, Ready, Waiting}

fn main() {
    print(json_stringify(Payload { name: "cross", values: [2, 3] }))
    print(json_stringify(Ready { code: 7 }))
    print(json_stringify(Waiting))
}
