use unspellable_identity_support::{project_marker}

fn vorton_slot_read() -> Int { 811 }

fn main() {
    print(project_marker())
    print(vorton_slot_read())

    let stored = map_from([("key", 97)])
    print(stored["key"])
}
