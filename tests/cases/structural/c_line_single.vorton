// The two decoys below prove that marker discovery ignores comments and strings.
// let c_line_marker_comment_decoy = 0

fn main() {
    let decoy = "let c_line_marker_string_decoy = 0"
    let c_line_marker_single = 41
    print("${decoy.len()}:${c_line_marker_single}")
}
