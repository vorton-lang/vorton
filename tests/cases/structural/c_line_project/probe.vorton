// let c_line_marker_project_comment_decoy = 0

pub fn project_line_value() -> Int {
    let decoy = "let c_line_marker_project_string_decoy = 0"
    let c_line_marker_project = 73
    if decoy.len() > 0 { c_line_marker_project } else { 0 }
}
