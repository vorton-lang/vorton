// A test declaration is its own pending-obligation transaction owner.

test "pending test owner" {
    let empty: Set<Int> = set_from([])
    assert(empty.len() == 0, "annotated empty Set should resolve")
    print("pending test owner: ok")
}
